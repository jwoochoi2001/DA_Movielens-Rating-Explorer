# 공통 작업 규칙

- 프로젝트 루트를 기준으로 상대경로를 사용한다.
- 실제 파일명과 열 이름을 확인한 뒤 작업한다.
- 원본 데이터는 data/raw/에서 읽고 수정하거나 덮어쓰지 않는다. 가공 데이터는 data/processed/에 둔다.
- Python 파일은 analysis/에, 그래프는 outputs/figures/에, 표는 outputs/tables/에 둔다.
- Python 코드는 재실행할 수 있게 작성하고 필요한 패키지와 실행 방법을 응답으로 안내한다.
- 제외하거나 대체한 데이터의 기준과 건수를 기록한다.
- 식별자를 양적 특성으로 해석하지 않고, 예측 시점 이후 정보를 부적절하게 사용하지 않는다.
- 요청하지 않은 Markdown 문서, 노트북, 중간 파일을 생성하지 않는다.
